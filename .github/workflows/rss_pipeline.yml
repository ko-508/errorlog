name: RSS Pipeline

on:
  schedule:
    - cron: '0 2 * * *'   # 毎日 11:00 JST（UTC 02:00）
  workflow_dispatch:        # 手動実行も可能

permissions:
  contents: write
  issues: write

jobs:
  pipeline:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: scripts/rss_requirements.txt

      - name: Install dependencies
        run: pip install -r scripts/rss_requirements.txt

      - name: Run RSS pipeline
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SCORE_THRESHOLD: '60'
          MAX_SCORE_CYCLE: '20'
        run: python scripts/rss_pipeline.py

      - name: Sync incident flags to posts (Phase 4)
        run: python scripts/incident_sync.py

      - name: Commit results
        run: |
          git config user.name "ErrorLog Bot"
          git config user.email "bot@users.noreply.github.com"
          git add scripts/rss_processed.json
          git add scripts/incident_flags.json 2>/dev/null || true
          git add content/posts/auto_*.md 2>/dev/null || true
          git add content/posts/*.md 2>/dev/null || true
          if git diff --staged --quiet; then
            echo "No new drafts"
          else
            git commit -m "rss: $(date +%Y-%m-%d) auto drafts"
            for i in 1 2 3; do
              git pull --rebase && git push && break || sleep 10
            done
          fi

      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              title: `🚨 ${context.workflow} 失敗 (${new Date().toISOString().slice(0,10)})`,
              body: [
                '## パイプライン停止検知',
                '',
                `- **ワークフロー**: \`${context.workflow}\``,
                `- **実行ログ**: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
                `- **失敗日時**: ${new Date().toISOString()}`,
                '',
                '確認後、このIssueをクローズしてください。',
              ].join('\n'),
              labels: ['pipeline-failure'],
            });

  notify:
    needs: pipeline
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Send publish notification
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          PUBLISH_NOTIFY_MAX_RETRIES: "10"
          PUBLISH_NOTIFY_RETRY_SECONDS: "60"
          PUBLISH_NOTIFY_FAIL_ON_ERROR: "false"
        run: python scripts/publish_notify.py --source-type rss --detect-from-commit

      - name: Commit notification history
        run: |
          git config user.name "ErrorLog Bot"
          git config user.email "bot@users.noreply.github.com"
          [ -f data/published_notification_history.json ] && git add data/published_notification_history.json || true
          if git diff --staged --quiet; then
            echo "No history changes"
          else
            git commit -m "notify: update rss publish notification history"
            for i in 1 2 3; do
              git pull --rebase && git push && break || sleep 10
            done
          fi
